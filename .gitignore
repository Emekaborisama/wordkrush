# Secrets — never commit. Real values live in .env; template in .env.example
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Expo / build output
.expo/
dist/
web-build/
ios/
android/
*.ipa
*.apk

# Test / tooling
coverage/
*.tsbuildinfo

# Python (validator/)
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# OS / editor
.DS_Store

# Finder "duplicate" copies (`engine 2.ts`, `lockup 2.png`, …). They are not
# product files; leaving them unignored makes the docs audit treat them as
# source changes.
* 2.*

# Supabase CLI scratch (version cache, etc.)
supabase/.temp/
