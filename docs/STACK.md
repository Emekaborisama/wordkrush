# Stack

**Project:** WordKrush — cognitive and pattern-recognition games for iOS and web.
**Status:** Multi-game Expo app with bundled content, optional Supabase accounts and global ranks, and Railway web deploys, plus a Reddit (Devvit) build of More or Less that shares the same engine. Game Center is still outstanding for native 4.2 evidence.
**Last updated:** 2026-08-30

This is a living document. Every stack change gets a row in the Decision Log at the bottom — including reversals. Do not silently rewrite a past decision; supersede it with a new entry so the reasoning trail survives.

---

## Current stack

| Layer | Choice | Notes |
|---|---|---|
| Language | TypeScript | Strict mode. Non-negotiable for the game logic layer. |
| App framework | Expo (React Native) | Chosen over Capacitor — see D-002. |
| UI | React Native core + `src/ui/theme.ts` tokens | Shared primitives in `src/ui/components/`. No third-party UI kit. |
| Web board input | `PanResponder` capture + scoped CSS in `src/ui/webViewport.ts` | Wordfall claims a trace at the board boundary; browser selection and touch actions are disabled only inside that board. No web-only gesture dependency. See D-056. |
| Motion (celebration / mascot) | `lottie-react-native` ~7.3 + `@lottiefiles/dotlottie-react` on web | Layout, gestures, count-up, and Wordfall match puff/stamp stay on RN `Animated`. Lottie is chrome only. `crush-hit` / `crush-best` remain ST-72. See D-032. |
| Sound + haptics | `expo-audio` ~57.0 (four bundled clips) + `expo-haptics` ~57.0 | Paired per game moment in `src/native/feedback.ts`; player can mute either channel. Clips are bundled assets, so audio never needs the network. See D-043. |
| State | React state + a pure reducer | Game engine is a pure function; see `docs/BRAINSTORM.md`. |
| Source layout | One `src/games/` root with one directory per title | Shared game infrastructure stays directly under `src/games/`. See D-024. |
| Typography | **Fredoka** (Medium/SemiBold/Bold) for every text tier | Bundled via `expo-font` + `@expo-google-fonts/fredoka`. Offline after first load. Symbol glyphs stay on the system face. See D-030. |
| Data (v1) | Static JSON bundled with the app | Play never requires a network. Card images are Wikimedia URLs chosen at snapshot time, never fetched as a content API. See D-004, D-045. |
| Unit tests | Vitest | Targets the pure logic layer only. |
| Build & signing | EAS Build (Expo cloud) | Removes manual cert/provisioning work. |
| Submission | EAS Submit → App Store Connect | |
| Distribution | TestFlight → App Store | |
| Targets | iOS + Web, one codebase | Via `react-native-web`. See D-006. |
| Product brand | **WordKrush** — [wordKrush.com](https://wordkrush.com) | Display name, Expo slug, npm package, and native bundle ids use wordkrush. Game ids and score keys stay stable. See D-028. |
| Brand kit | Lockup (black, clear, SVG) + W mark in `assets/logo/`; identity in `docs/branding/` | Splash uses the black lockup; auth/Android foreground use the clear lockup; app icon/favicon use the mark. Umbrella accent is lockup gold (`brand.krush`). See D-031. |
| Product goal | Strengthen cognitive skills and pattern recognition through repeated play | Player promise: "Guess correctly, keep the streak alive, and beat your best score." See D-028. |
| Content DB | Supabase Postgres | Content **factory** for snapshots — the app still ships bundled JSON. See D-007. |
| Optional runtime backend | Same Supabase project, publishable key only | Accounts via **email magic link**, unique leaderboard username, `global_scores`, private teams, and live matches. Offline play and local boards survive if it is missing. See D-016, D-025, D-033, D-037, D-048. |
| Data sourcing | **Wikimedia pageviews** via `pipeline/sources/wikipedia.ts` | Free, measured, no auth. Resolves O-2. See D-012. |
| Pipeline runtime | Node + `tsx`, `npm run pipeline:*` | Reads `.env` locally; scripts in `pipeline/`. |
| CI | GitHub Actions | Documentation impact + typecheck + tests, plus Expo web export, on every push/PR. Deploy waits for both jobs. Every PR is a version bump; merge to `master` (or tag `vX.Y.Z`) publishes the GitHub Release. Labeled Clueless and Wordfall content drafts are marked ready and merge only after their exact head passes CI. Official JavaScript actions use Node 24-native releases. Monday cron re-measures Wikipedia popularity and enqueues a new label round via a review PR. Tuesday 09:00 UTC cron sends the player Broadcast via Resend. See D-020, D-029, D-036, D-039, D-041, D-052, D-053, D-054, D-059, D-060, D-062. |
| Documentation drift | Dependency-free Node audit + Cursor stop hook | Detects missing path-based documentation updates locally and in CI; never generates prose. A version-only `package.json` / `app.json` bump is changelog, not stack. See D-022, D-049. |
| Web hosting | **Railway** — project `best-games`, service `wordcrush` | Public site is [wordKrush.com](https://wordkrush.com). Auto-deploys from CI on green `master` after `check` and `web`. Railway runs `CI=true npm run build:web` (`expo export` then `scripts/patch-web-head.mjs` so the tab icon is the WordKrush W PNG, not a cached `/favicon.ico`; Expo's `getenv` rejects an empty `CI`) and serves `dist/` via `server/serve.mjs`. The same patch step emits robots, sitemap, JSON-LD, and crawler-readable hub copy. Config in `railway.json`. See D-020, D-028, D-029, D-061. |
| Product analytics | PostHog React Native, explicit opt-in | Typed events behind `src/analytics/`. Guests stay anonymous; after consent, signed-in players are identified with account id, username, and email. No autocapture, session replay, or guessed content. See D-022/D-024/D-040. |
| Player feedback | **Userback** hosted widget (`@userback/widget`), web build only | Bug reports and suggestions with screenshot annotation, behind `src/userback/`. Launcher shows on every non-game screen and in the drawer; hidden during a run. Signed-in players are identified so a reply can reach them, guests stay anonymous, and session replay is never started. Native is a no-op twin until a Userback Mobile SDK key exists. See D-046. |
| Player email | **Resend Broadcasts**, Tuesday 09:00 UTC | Pipeline reads player-facing changelog bullets + this week’s Wordfall, drafts player copy once with OpenRouter (`openai/gpt-4o-mini`, `fetch`, no `openai` package), personalizes via Resend merge tags, and skips quiet weeks. Auth templates stay sign-in-only. Secrets live on GitHub Environment `best-games`. See D-053, D-054, D-062. |
| Content validation | Python 3.13 + Pydantic v2 + FastAPI + OpenAI structured outputs (`validator/`) | Offline **playability** referee, never a game runtime dependency. See D-010, D-014. |
| Reddit surface | **Devvit Web** app in `reddit/` — More or Less only | Its own npm project (React 19 + Vite + Hono, Node ≥24). Imports the engine and snapshot from `src/` rather than forking them; server owns the run, Devvit Redis holds the board. A cron task posts daily at 13:00 UTC. See D-042 and [reddit/README.md](../reddit/README.md). Root `npm test` runs its pure **and** server-route suites without installing `reddit/node_modules`: the Devvit runtime is aliased to an in-memory fake in `vitest.config.ts`, and `hono` is a root devDependency pinned to the version `reddit/package.json` ships, so the route tests exercise the real router. CI installs the root tree only. |
| Task board | Superthread — *BestGame* board | Board of record for status; cards follow the task contract in [WORKFLOW.md](WORKFLOW.md). [ROADMAP.md](ROADMAP.md) mirrors the work for repo-only readers. See D-011/D-015. |
| Agent runtime | Cursor Cloud Agents (Grok) + project skills; GitHub Actions remain CI/deploy/content-merge/email/Wikipedia | Judgment work (content, organic X growth, QA, product health) runs as specialist Grok bots. GitHub Actions stay the deterministic hands. A bot does not replace CI and does not push to master. See new D-id below, [AGENT-OS.md](AGENT-OS.md). |
| Recurring content review | Standing content branches | Wikipedia remains human-reviewed. Clueless and Wordfall bots use `automation:auto-merge`; the GitHub Action marks eligible drafts ready and accepts only their named content branches after CI succeeds for the exact current head. No loop pushes directly to `master` or bypasses a failed or pending check. See D-036/D-052/D-057/D-058/D-059/D-060. |

## Local environment

| Tool | State as of 2026-08-16 |
|---|---|
| macOS | Darwin 25.6.0 |
| Node | v25.2.1 |
| npm | 11.6.2 |
| Xcode | **Downloading** — not yet installed. `xcodebuild` absent, no simulator runtimes registered. |
| Apple Developer Program | Not yet purchased. $99/yr, required to submit. |

### Post-install steps for Xcode

```bash
sudo xcode-select --switch /Applications/Xcode.app
sudo xcodebuild -license accept
xcodebuild -runFirstLaunch
```

Then open Xcode once and download a simulator runtime via Settings → Components. The App Store download does not reliably include one, and it is a separate multi-GB fetch.

## Testing strategy

Four rungs, cheapest and fastest first. Most work happens on rungs 1–2.

1. **Vitest on pure game logic** — pairing, scoring, streak, difficulty. No React, no native, no I/O. Runs in milliseconds. This is where real coverage lives.
2. **Expo Go on a physical iPhone** — `npx expo start`, scan QR, hot reload. The daily feel-it loop. No Xcode required.
3. **iOS Simulator** — screen sizes we don't own, safe-area/notch behavior. Requires Xcode.
4. **TestFlight** — real testers, real builds, real devices.

Xcode is deliberately *not* on the critical path: EAS builds run in Expo's cloud, and day-to-day testing runs through Expo Go.

## Constraints that shape the stack

- **App Store Guideline 4.2 (minimum functionality).** Apple rejects apps that are a website in a wrapper. This is the single biggest reason we are not using Capacitor. To stay clearly on the right side of it, v1 should ship at least: offline play, haptic feedback, and a native leaderboard (Game Center). The current Supabase global board is a cross-platform rank list, not that native proof.
- **No backend required to play.** Bundled data means zero hosting cost, zero latency, offline play, and deterministic tests. Content updates still require an app release (D-004). Optional accounts and global ranks use the same Supabase project as the content factory, behind `EXPO_PUBLIC_*` plus RLS (D-016). A missing or failing backend leaves local scores intact.
- **The logic layer must never import React or React Native.** It is plain TypeScript so it stays testable in Node and portable if the UI layer ever changes.

## Open decisions

| # | Question | Blocking? |
|---|---|---|
| ~~O-1~~ | ~~Which category ships first?~~ | **Resolved 2026-08-16: Wikimedia pageviews ("Popularity"), owner's call.** More categories later. |
| ~~O-2~~ | ~~Which real data source for search volume?~~ | **Resolved 2026-08-16 → D-012: Wikimedia pageviews.** Paid keyword APIs remain a later upgrade if true Google volumes justify the cost. |
| ~~O-6~~ | ~~Web host for the static export?~~ | **Resolved 2026-08-18 → D-020: Railway**, owner's call. Live at `https://web-production-548e8.up.railway.app` |
| O-3 | Lives (3 hearts) vs. single-life endless streak? | No — engine supports both |
| ~~O-4~~ | ~~Does the app need a web build too, or iOS only?~~ | **Resolved 2026-08-16 → D-006: both, one codebase** |
| O-5 | Monetization: ads, IAP, or free? | No — out of scope for v1 |

## Decision log

| ID | Date | Decision | Rationale | Status |
|---|---|---|---|---|
| D-001 | 2026-08-16 | TypeScript everywhere | Shared language across logic, UI, and tooling; the logic layer needs static types to stay refactorable. | Active |
| D-002 | 2026-08-16 | Expo (React Native) over Capacitor, Swift, and Phaser | Capacitor renders a WebView → real Guideline 4.2 rejection risk, plus manual signing. Swift means learning a new language mid-project. Phaser is a canvas engine and would need a WebView anyway. Expo gives real native UI, automated signing via EAS, and instant device testing. | Active |
| D-003 | 2026-08-16 | EAS Build + EAS Submit for the release pipeline | Code signing and provisioning are the hardest part of shipping to the App Store, and EAS automates both. Cloud builds also keep Xcode off the critical path. | Active |
| D-004 | 2026-08-16 | v1 data is static JSON bundled in the app | No backend to run or pay for; enables offline play (helps the 4.2 case) and makes tests deterministic. Cost: content updates need an app release. | Active — revisit when content churn becomes painful |
| D-005 | 2026-08-16 | Vitest over Jest | Faster, native ESM and TypeScript, and we only test the pure layer so React Native's Jest preset buys us nothing. | Active |
| D-006 | 2026-08-16 | Ship iOS **and** web from one Expo codebase via `react-native-web` | `src/game/` and `src/data/` are already platform-free, and the UI is simple enough that RN primitives map cleanly to DOM. Adds a fast browser testing loop that needs neither simulator nor phone. Cost: larger bundle and weaker SEO than a purpose-built web app — acceptable for a game behind a Play button. Split into a monorepo (shared logic package + Next.js web) only if web becomes a marketing/SEO surface. | Active — branded search surface is D-061, not a Next.js split |
| D-007 | 2026-08-16 | Supabase Postgres is the content **factory**, not a runtime dependency | Owner supplied Supabase keys and proposed batch-validating data into Postgres — adopted, with one boundary: the app still ships bundled JSON snapshots (D-004 stands). DB holds full provenance (snapshots, raw responses, flags); app gets a clean export. Preserves offline play, zero client keys, deterministic tests. Supabase may later serve leaderboards/daily challenges — separate decision. | Active for content — runtime accounts and ranks are D-016 |
| D-008 | 2026-08-16 | Data sources are pluggable adapters; ship the pipeline on a deterministic mock first | There is **no official public Google search-volume API**, so the source choice (O-2) involves cost/licensing trade-offs the owner must make. The adapter interface (`pipeline/sources/types.ts`) makes that choice a one-file swap instead of a blocker: the whole ingest→validate→store→export chain is provable end-to-end today with mock data. | Active |
| D-010 | 2026-08-16 | LLM (OpenAI) is a **referee** over content, never a source of search-volume numbers | Owner proposed OpenAI for validation — adopted, with a hard boundary. Models hallucinate absolute figures ("pizza gets 3.2M searches") with total confidence, so asking for numbers would manufacture fake facts and then validate them circularly. Relative pairwise judgement ("is pizza searched more than sushi") is a different task models are genuinely good at. So: constrained enum schema, both orderings checked for position bias, flag only confident contradictions with our data. Python chosen over TS here because Pydantic + OpenAI structured outputs is best-in-class and the pipeline is language-independent from the app. | Active |
| D-012 | 2026-08-16 | **Wikimedia pageviews is the v1 data source**; the metric is "monthly Wikipedia pageviews", never "Google searches" | Owner asked whether a web-search-grounded LLM could supply real numbers. Tested, not assumed: a bare prompt returned a stable 13.6M for "pizza", but demanding a citation made two runs disagree on every one of five terms, and the stable figure traced to an SEO farm quoting **Etsy** keyword data. Web search relocates the failure from hallucination to garbage-in. Wikimedia instead publishes *measured* pageviews — free, no auth, CC-licensed, batch-friendly. Cost: pageviews track encyclopedic, not commercial, interest, so the label must state exactly that. Enforced by a test asserting no category claims "Google". | Active |
| D-013 | 2026-08-16 | Median monthly pageviews over 6 **complete** months, not mean | Found by inspecting real output. (1) Including the in-progress month pulled in a partial count (August showed 33,711 on day 16), deflating every item by an amount depending on the run date. (2) Pageviews spike hard on events — Messi hit 5.85M during the 2026 World Cup vs a ~500K baseline — and the mean let one month define an item for the whole window. Median reports a typical month and ignores the tail; sustained shifts (Michael Jackson's 3-month biopic elevation) still come through, which is correct. | Active |
| D-014 | 2026-08-16 | Keep the OpenAI validator, but narrow its job from **truth-checking** to **playability-checking** | With Wikimedia (D-012) the numbers are measured truth for their own metric, so there is nothing left for an LLM to "verify" — asking it to would be theatre. Its remaining job is real and different: pageviews are a *proxy* for popularity, and where the proxy diverges from what a player would expect, the card is unfair even though the data is correct. The LLM is a cheap stand-in for player intuition; a confident LLM/data disagreement means "this pair will feel wrong", not "this number is wrong". The `bitcoin vs sushi` WARN in the smoke suite is the standing example. Also still earns its keep on mis-resolved articles (e.g. a title that means something else to players). | Active |
| D-011 | 2026-08-16 | Superthread *BestGame* board is the task board of record; `docs/ROADMAP.md` mirrors it | Owner's call. Board holds live status; the doc holds the work breakdown, blockers, and ordering so an LLM reading the repo cold needs no board access. Doc also records the Superthread API gotchas (team-scoped paths, required `project_id` query param, Cloudflare UA ban). | Active |
| D-015 | 2026-08-16 | Superthread cards use a compact agile task contract | Each card should state the outcome, context, scope boundaries, acceptance criteria, verification, and sign-off so collaborators can work without guessing or widening scope. Keep the board Kanban-style; no sprint ceremony required. | Active |
| D-021 | 2026-08-22 | Every feature and fix is a PR; the branch name is the Superthread card's `suggested_branch_name`; stacked PRs are allowed | Owner's call. Invented `feat/<slug>` names break the GitHub ↔ Superthread link (the card ID in the suggested name is what auto-links the branch and PR). Direct pushes of features or fixes to `master` skip review and that link. A follow-on task may target an unmerged parent branch instead of `master`, but each PR in the stack still uses its own card's branch name. | Active |
| D-022 | 2026-08-22 | Product analytics is anonymous and disabled until explicit consent; signed-in status may be measured, but PostHog is not joined to account identity | The goal is to improve every player-facing area without turning optional accounts into a tracking requirement. Bounded events and an `auth_status` cohort answer activation, retention, balance, and reliability questions while excluding email, username, profile ids, guessed content, autocapture, and session replay. This preserves offline play and keeps refusal consequence-free. True cross-device analytics would require a later decision covering linkage, deletion, and revised consent. PostHog is the proposed product-analytics destination, but SDK selection and deployment remain a follow-on implementation. See `docs/OBSERVABILITY.md`. | Superseded by D-040 |
| D-023 | 2026-08-22 | The product brand is **WordCrush**, with the player promise "Guess correctly, keep the streak alive, and beat your best score" | The name now covers the comparison and word-game collection under one identity. The product purpose is to create engaging repeated play that strengthens cognitive skills and pattern recognition. Existing technical identifiers such as `more-or-less`, `bestgames.*` storage keys, the Expo slug, and the iOS bundle id remain stable so the branding correction does not lose scores or create a new deployed app. | Superseded by D-028 |
| D-024 | 2026-08-22 | PostHog React Native implements the D-022 analytics policy through a typed, consent-gated adapter | The standalone client works across Expo native and web with the existing AsyncStorage dependency. It starts opted out, creates no person profiles, disables SDK lifecycle capture, remote flags, push capture, error autocapture, and session replay, and drops event names outside the documented dictionary. UI and persistence boundaries emit bounded properties; pure game reducers remain untouched. Railway and EAS receive only the public project token and ingestion host at build time. | Active; person-profile clause superseded by D-040 |
| D-024 | 2026-08-22 | Keep one game-code root: `src/games/`, with one directory per title | The original More-or-Less engine predated the multi-game hub and lived in singular `src/game/`, while newer titles lived in plural `src/games/`. Co-locating each title's engine, types, persistence validation, and tests under its stable game id removes that ambiguity. Cross-game modules such as the registry, seeded RNG, and progress adapter stay directly under `src/games/` so one game never depends on another game's internals. | Active |
| D-016 | 2026-08-22 | Supabase is an **optional runtime** for accounts and ranks; play and local scores stay offline | The publishable URL/key (`EXPO_PUBLIC_SUPABASE_*`) may be omitted. When present, the client uses RLS on `players`, `leaderboard_entries`, and `global_scores` — never the secret key. D-007 still owns content snapshots. A down or unconfigured backend must not block a run or drop AsyncStorage history. | Active |
| D-025 | 2026-08-22 | Scores expose **both** a global leaderboard and a local device board | Owner's call. Local history is the source of truth and works for guests. Global shows one best per signed-in player per `game_id` (`global_leaderboard`), with Clueless sorted lower-is-better in SQL. Uploads are idempotent and retried from unsynced local rows; they never replace the device board. Game Center remains a later native 4.2 track. | Active |
| D-026 | 2026-08-22 | Display type is bundled **Fredoka**; body copy stays on the system face | `expo-font` plus `@expo-google-fonts/fredoka` (500/600/700) load in `App.tsx` before the hub. Rounded display type matches the WordCrush arcade direction; small body text stays on SF Pro / the platform default so captions stay readable. The faces ship in the bundle, so titles do not depend on a font CDN at play time. | Superseded by D-030 — Samurai Blast is personal-use only and is not bundled until a commercial license file is added |
| D-027 | 2026-08-22 | Wordfall releases a new bundled level each Monday | Owner's call. Levels stay in `src/data/wordfall/levels.ts` with `availableFrom` set to that Monday. The launch curriculum omits the date so it is playable immediately. The picker time-gates on the player's local calendar; no remote level fetch (D-004 stands). Beating the current last level unlocks the next slot so a later drop does not require replaying the campaign. | Active |
| D-028 | 2026-08-22 | The product brand is **WordKrush**, public site **wordKrush.com** | Owner renamed from WordCrush. Display name, Expo slug, npm package, iOS bundle id (`com.emekaborisama.wordkrush`), Android package, and deep-link scheme update now, before first App Store submission. Game ids (`more-or-less`, `clueless`, `wordfall`) and `bestgames.*` score keys stay stable so local history survives. The analytics consent key follows the brand, so existing devices will be asked once more. Wikimedia User-Agent contact URL is https://wordkrush.com. Railway remains the host (D-020); wordKrush.com is the player-facing address. The player promise from D-023 is unchanged. | Active |
| D-029 | 2026-08-22 | CI must pass an Expo web export before Railway can deploy | Extends D-020: `check` still gates merge, but a PR that typechecks can still fail `expo export --platform web`. Running `npm run build:web` as a sibling GitHub Actions job fails that case before merge. Deploy `needs: [check, web]`. Local `npm run check` stays docs + typecheck + tests so the daily loop stays fast. Validator, smoke, lint, and bundle budgets stay out. | Active |
| D-030 | 2026-08-22 | **Fredoka is used for every text tier**, in all three games | Owner's call, superseding D-026's display-only split. `type.body`, `type.bodyStrong`, and `type.caption` now name a Fredoka face, so the app reads as one typeface rather than two. Ad-hoc styles that set `fontSize`/`fontWeight` without a `fontFamily` were given the matching face. EXCEPTION: symbol glyphs (✓ ✎ ◎ ⟷ ▤ ★ ≡, Wordfall special-tile marks) keep no `fontFamily` — Fredoka is a Latin display face that does not contain them, so naming it would force a silent per-platform fallback or render tofu. `TextInput` is a second gap: it is not a `Text` subtree, so `TextField` names the face on the input and, on web, overrides RN-web's `font: 14px System` reset. Chrome/Safari `:focus-visible` still paints a sharp ring unless a real stylesheet rule (not an RN class) sets `outline: none`. D-026's legibility concern is real and is the thing to re-check if small copy is reported as hard to read. | Active |
| D-031 | 2026-08-22 | Brand kit is `assets/logo/` + `docs/branding/`; umbrella accent is lockup gold | Owner supplied the WordKrush lockup. The W mark stays the app icon because the lockup is unreadable below ~120px. Splash and auth hero use the lockup; hub/drawer/top bar use the mark beside the name. `theme.accent` aliases `brand.krush` (`#FFB020`) so chrome matches the wordmark instead of the old pink. Game registry accents are unchanged. | Active |
| D-032 | 2026-08-22 | Lottie for mascot and later celebration clips; owner allows a lottie.host CDN URI with a bundled fallback | Owner supplied a little-deer `.lottie` and asked to load it from [lottie.host](https://lottie.host/35f01f32-2f23-42a1-b228-6d7b5b86d50a/RrWseXVzN1.lottie) as well as from disk. `lottie-react-native` (Expo 57, Expo Go) plus `@lottiefiles/dotlottie-react` (web peer) play both JSON and `.lottie`. Metro adds `lottie` as an asset extension. The CDN is chrome, not game data: a miss falls back to `assets/lottie/deer.lottie` and never blocks a run. D-004 still holds for categories, Wordfall levels, and dictionaries. Do not import Lottie from `src/games/`. `@lottiefiles/dotlottie-react-native` was rejected because it needs a custom dev client and would break the Expo Go loop. Wordfall tile puff, special-birth pop, and Crush/Nova stamps stay on RN `Animated` (ST-80); the empty `crush-hit` / `crush-best` slots remain ST-72. | Active |
| D-033 | 2026-08-22 | Optional accounts sign in with **Supabase magic link**, not a password | Owner's call, superseding the ST-61 email+password form. Same Supabase Auth project and publishable-key client (D-016). `signInWithOtp` emails a link (`emailRedirectTo` is the current web origin or the Expo deep link). Web lets supabase-js parse the redirect; native exchanges the PKCE code from `wordkrush://` / Expo Go `exp://`. The 6-digit code in the same email is a fallback when the link cannot open the app. Create-account still collects a leaderboard username into user metadata; sign-in does not silently create users. No new auth provider. Google/X OAuth remains ST-62. | Active |
| D-034 | 2026-08-22 | Phone SMS OTP is a second Supabase sign-in channel | Owner enabled the Phone provider on the same Auth project. `signInWithOtp({ phone, options: { channel: 'sms' } })` then `verifyOtp({ type: 'sms' })`. Numbers must be E.164 (`+` and country code). Phone numbers are not sent to PostHog (D-022). Email magic link (D-033) remains. SMS delivery depends on a provider configured under that Phone settings page (Twilio or equivalent); enabling the toggle alone does not send texts. WhatsApp is out of scope. | Superseded by D-037 |
| D-035 | 2026-08-22 | Local AI test player lives in `.env` as `TEST_PLAYER_*`, never in the client bundle | Owner asked for a shared account agents can use to test. It is a confirmed ordinary Auth user created with the secret key (`npm run auth:ensure-test-player`), not an RLS-bypass superuser. `SUPABASE_SECRET_KEY` remains the only privileged credential. Names go in `.env.example`; values stay in gitignored `.env`. Never `EXPO_PUBLIC_*`, Railway, or EAS. Player-facing auth stays magic link / SMS. | Active |
| D-018 | 2026-08-18 | Wordfall's word list is built from the **public-domain** Webster's 1913 (`/usr/share/dict/web2`), with inflections derived at lookup rather than stored | A word game lives or dies on recall: rejecting a word the player knows is the worst failure mode it has, so the accept list needs breadth we cannot hand-author. Webster's 1913 is public domain and ships with macOS/BSD, which settles the rights question that blocks most word lists (agents.md: no bundling without confirmed rights). It has one systematic gap — it lists lemmas, so "cat" is present and "cats" is not. Generating every inflection up front cost 995 KB of bundle for words nobody would ever trace; suffix-stripping at lookup gets the same words for 550 KB **and** better recall, since it also resolves forms a generator would not have produced. Remaining gaps (irregulars like "held"/"women", plus "box", plus the modern lexicon) are patched by a short hand-checked list. Cost: over-accepts at the edges ("informationed" resolves) — the correct direction to be wrong in. Build is `npm run pipeline:wordfall`; it reads local system files, so **its output is committed and CI never regenerates it**. | Active |
| D-019 | 2026-08-18 | Wordfall separates the **accept** list from the **everyday** list, and derives letter spawn weights and level targets from the latter | One word list cannot do both jobs. The board generator certifies a board by counting the words findable on it — if it counted Webster's obscurities it would happily certify boards whose only solutions are "aalii" and "abaft": solvable on paper, dead in the hand. So the solver and the rarity ranking use the ~6k frequency-ordered everyday words (reusing the vocab already bundled for Clueless rather than shipping a second one), while validation uses all 76k. The same everyday list supplies the measured per-letter spawn weights, so the board is stocked in the proportions real words actually use instead of a hand-tuned guess. Level score targets were then calibrated by simulating a perfect solver over many seeds — the first guesses were wrong in both directions and the data moved them. | Active |
| D-020 | 2026-08-18 | **Railway hosts the web build**, deployed from CI only after `check` passes, and served by a zero-dependency Node static server rather than a hosting platform's CDN defaults | Resolves O-6 (owner's call). Railway builds from source (`railway.json` → `npm run build:web`) so the deployed artifact is always reproducible from the repo, and `server/serve.mjs` is the ~150 lines that turn a folder of files into a process on `$PORT`. Written by hand rather than adding `serve` or similar, per the standing rule to prefer platform APIs — and because the two things that actually matter here need explicit control: the bundle is a single ~3 MB JS file, so it is pre-compressed with brotli at boot (2.89 MB → 0.77 MB over the wire), and Expo hashes that filename while `index.html` points at it, so the two need opposite cache policies (`immutable` vs `no-cache`). Getting the second one backwards pins users to a stale build after every deploy and fails silently. Deploy is gated on the `check` job because the definition of done is "npm run check green", and a host that ships a red build makes that a suggestion. Cost: one `RAILWAY_TOKEN` secret in GitHub. Alternative considered: Railway's GitHub App auto-deploy, which needs no secret but deploys regardless of test results. | Active |
| D-022 | 2026-08-22 | Enforce documentation impact with one dependency-free changed-file audit shared by local checks, CI, and a Cursor completion hook | Documentation had explicit update rules but still drifted because nothing enforced them. Automatic prose generation would manufacture decisions, while an edit-time hook would add noise and token cost. The audit therefore checks only the path-based minimum, reports the exact missing canonical document, and runs once at agent completion plus in `npm run check`. Semantic design and task-status impact remains a human/agent judgement. | Active |
| D-036 | 2026-08-22 | **Weekly Wikipedia popularity refresh is a GitHub Actions cron that opens a PR**, not a runtime fetch and not an auto-merge | Owner asked to rotate the Popularity snapshot every week. D-004 still holds: the client reads bundled JSON. The job re-runs the existing preview builder (pageviews + free-licence images) because factory ingest→export is blocked on the 0001 migration (ST-35). It compares against the shipped file so an unchanged month is a no-op, fails closed below 20 playable items, and lists >10x swings on the PR. Merge stays human — the JSON diff is the content review. The standing branch `content/wikipedia-popularity-weekly` is an automation exception to D-021 (no Superthread card per week). Wikimedia needs no secrets. `GITHUB_TOKEN` PRs do not retrigger CI, so the workflow runs `npm run check` before opening the PR. | Superseded in part by D-052 |
| D-037 | 2026-08-22 | One unique leaderboard username per account; email magic link is the only sign-in channel | Owner's call. Email and phone OTP minted separate Auth users that could reuse the same `display_name`, so two people (or one person twice) could appear as the same name on the board. Username stays public identity, not a login identifier. Create account is username + email; sign-in is that email. `0004_unique_username.sql` unique-indexes `username_key(display_name)` (trim, collapse spaces, lower-case). Phone/SMS as a second identity is dropped (D-034 superseded). Identity linking and username-as-login stay out. OAuth remains ST-62. Magic link (D-033) is unchanged. | Active |
| D-038 | 2026-08-22 | Weekly Wordfall authoring tests locally and serves the web export before GitHub; each drop is a unique hard task | Owner's call. Job B (skill `wordfall-weekly-gauntlet`) must pass Vitest catalog/engine tests, `npm run check`, `npm run build:web`, `npm run serve:web`, and a picker playtest of that export before `git push`. `npm run web` is not that gate. Unique task = `taskFingerprint` (puzzle vs race plus kinds), never last week's question, never a sixth `Objective` kind. Featured TTL is seven days (`isNewestRelease`); the catalog row stays so D-027 unlock numbering has no holes. D-027 (bundled Monday `availableFrom`, no remote fetch) is unchanged. | Active |
| D-039 | 2026-08-22 | GitHub Release `vX.Y.Z` is published from the changelog, not by hand | Owner asked to automate `gh release` for the first version (`0.1.0`). The notes are the Keep-a-Changelog section humans already wrote (`scripts/changelog-notes.mjs`). Merging a rolled `[x.y.z]` to `master`, or pushing tag `vX.Y.Z`, creates the GitHub Release with `GITHUB_TOKEN`. Idempotent if the tag already exists. Does not bump versions, merge PRs, or call EAS/App Store (D-003 still needs owner accounts). Web still deploys from CI on `master` (D-020). | Active |
| D-040 | 2026-08-22 | After consent, signed-in players are identified in PostHog | Owner asked to add user sign-up to PostHog. D-022 kept accounts unjoined so optional sign-in was not a tracking requirement; that blocked person profiles because `allowlistedCapture` dropped `$identify`. Guests stay anonymous. After opt-in, sign-up and sign-in call `identify` with the Supabase user id plus `name`, `username`, and `email`. Consent copy now says so, and the stored consent key is bumped to `v2` so earlier anonymous grants are re-asked. Autocapture, session replay, guessed content, and unsigned-in identification stay off. Sign-out still `reset`s. Account deletion must also delete the PostHog person. See `docs/OBSERVABILITY.md`. | Active |
| D-041 | 2026-08-23 | Every PR is a version; changelog headings are never edited in place | Owner asked to stop accumulating under `[Unreleased]` and stop adding features or dates to a shipped version. Each PR that lands on `master` bumps `package.json` / `app.json` and adds a new `## [x.y.z] - date` section. Patch by default; minor or major when the change warrants it. Merge still publishes GitHub Release `vX.Y.Z` from that section (D-039). The weekly Wikipedia rotate PR does the same patch bump. Do not append bullets to an already-shipped heading. | Active |
| D-042 | 2026-08-23 | **More or Less also ships as a Reddit app** (`reddit/`, Devvit Web), sharing the engine and snapshot rather than forking them; the server owns the run | Owner's call, taken as a distribution experiment and not a migration. GTM §1 diagnoses "a content engine and no distribution loop" and lists four Phase-0 blockers; Devvit makes all four irrelevant rather than solving them — the post *is* the share, nothing links out so previews do not apply, Reddit threads carry their own ranking, and it runs in the Reddit app on every phone with no Apple Developer Program. **More or Less, not Clueless:** a daily word puzzle has a spoiler problem — the comment section fills with the answer and poisons the post for later readers — while More or Less has no single answer to spoil, is one tap, and produces disagreements people want to argue about. Feasibility was checked, not assumed: `src/games/more-or-less/` imports nothing but its own types and the seeded RNG, so `reddit/src/shared/` imports the engine and `wikipedia-popularity.json` directly and `tools/tsconfig.shared.json` names every crossing file, failing the build with TS6307 if the boundary widens. Only the view layer is new. **The server owns the run:** the client is never sent the seed or a hidden value and never asserts a score — it posts one word per round and the server judges it, which removes SEC-01's "any number a client sends" problem on this surface for free, and is verified by the build (neither client bundle contains the pool, the values, or the engine; splash is 1.5 KB). Only a player's first completed run is recorded, because everyone shares a sequence and a replay knows the answers. A cron task posts daily at 13:00 UTC from a date-derived seed, so D-036's weekly refresh is the whole content calendar. **Costs, accepted:** a second surface to maintain that will drift; no Supabase auth, no global leaderboard, no cross-game streak, no PostHog; and Reddit owns the audience and the terms — there is no email, no install, and no direct relationship with a player. D-004 (bundled data, no remote fetch at play time) is unchanged and matches Devvit's model. | Active |
| D-009 | 2026-08-16 | Deployment-first: scaffold + CI + EAS profiles + pipeline before features | Owner's call: all config up front so future work is features-only. GitHub Actions runs typecheck+tests on every push; eas.json defines dev/preview/production; release process documented in WORKFLOW.md. Remaining setup steps need owner accounts (Expo login, Apple Developer, Supabase migration, web host) — listed in WORKFLOW.md checklist. | Active |

| D-043 | 2026-08-23 | **Sound effects and haptics are paired per game moment in one table** (`src/native/feedback.ts`), with `expo-audio` added as the playback dependency and both channels player-mutable | Owner asked for sound and vibration. Four clips were supplied (`assets/sounds/`), so the question was where the mapping lives. Screens name the moment (`feedback('levelUp')`), never the clip or the haptic style: that keeps the game's whole feel retunable from one table instead of three screens, and puts the mute switches in one place rather than at every call site. `expo-audio` is the current Expo audio module (`expo-av` is retired) and plays on iOS, Android and web from the same file, so unlike haptics it needs no platform twin. Clips are bundled, not fetched, holding the D-004 offline line. Audio mode is set so the iOS silent switch still silences the game and other apps' music keeps playing — a game that overrides the hardware mute switch is a support complaint, not a feature. Haptics stay a deliberate no-op on web (D-002 rationale unchanged), so the vibration switch is hidden there rather than shown doing nothing. | Active — the web-haptics clause is narrowed by D-044 |

| D-044 | 2026-08-23 | **Web vibration is capability-detected, not assumed off** — `navigator.vibrate` plus `(pointer: coarse)`, narrowing D-043's "no-op on web" clause | Owner reported vibration missing, and on the web build it genuinely was: the haptics web twin was five empty no-ops. The inherited justification was that the Vibration API "is unsupported in Safari and feels wrong on a laptop" — both true, and both now enforced by the detection rather than by a blanket off. Neither describes Android Chrome on a phone, which supports `navigator.vibrate` and where a buzz is exactly right. D-042 sends players to wordkrush.com from Reddit, so the phone browser is a primary surface, not an afterthought. `navigator.vibrate` absent rules out Safari and every iOS browser (all WebKit); `(pointer: coarse)` rules out laptops and desktops, where Chrome exposes `vibrate` and silently does nothing. The drawer's vibration row is gated on the same `canVibrate()` rather than on `Platform.OS`, so the switch appears exactly where it can fire. The rest of D-043 — the one feedback table, the bundled clips, the silent switch behaviour — is unchanged. | Active |
| D-045 | 2026-08-23 | **Card images fall back to other freely-licensed files on the Wikipedia article when the lead is non-free** | Playtest showed blank TikTok / Netflix / PlayStation cards. `pageimages` (free-only) returns nothing when the infobox is a fair-use logo or screenshot; those articles still have CC/PD photos in the body. The builder still allowlists licences and never ships fair-use. URLs stay remote Wikimedia thumbs recorded at snapshot time (D-004, D-036); bundling bits locally is ST-56. | Active |
| D-046 | 2026-08-23 | **Player feedback is collected by Userback's hosted widget on web**, loaded from `src/userback/` behind a platform twin, with signed-in players identified | Owner asked for a way to get feedback from players and supplied the Userback project. The alternative — an in-app form writing to Supabase — was not taken: it would need a table, a migration, RLS, moderation and somewhere to read the results, to arrive at a worse version of a product Userback already runs, screenshot annotation included. Costs accepted: a third-party script on the web build, and player reports living outside the owner's database. **Web only.** `@userback/widget` is a browser SDK — it injects a `<script>` tag and reaches for `document` — so `widget.native.ts` is a no-op twin in the `native/haptics.ts` pattern rather than a crash on iOS. Userback's native path is a separate `@userback/react-native` package (Beta) with its own Mobile SDK key, which does not exist yet; the drawer hides the entry there instead of offering a dead one. **Identity, not tracking:** `toUserbackIdentity` is pure and tested, signed-in players are sent id/username/email so a reply can reach them, and guests are deliberately left anonymous rather than given a generated id — a stable anonymous id would turn a support tool into a tracker, which is not what D-022 consent covers. `startSessionReplay` is never called for the same reason; nothing leaves the browser until a player types a report and presses send. The widget does load regardless of the analytics choice, because it is user-initiated support rather than measurement. The launcher is hidden during a run (`setUserbackLauncherVisible`), matching the existing rule that the game screen owns the whole viewport. Widget styling stays dashboard-owned — no `widget_settings` in code. `EXPO_PUBLIC_USERBACK_TOKEN` is a public project token like PostHog's; unset turns the feature off rather than half-loading it. | Active |
| D-047 | 2026-08-24 | **Clueless keeps one stable game id but partitions scores and global ranks by difficulty context** | Owner chose Easy, Standard, and Expert over a campaign because player feedback says the blank daily puzzle is too hard. Assistance makes raw guess counts incomparable: Easy sees a thematic sentence immediately, Standard can see it after 15 valid guesses, and Expert never does. New runs therefore store the mode in the existing `context_id`; local history filters by it and the SQL view partitions Clueless by `(game_id, context_id)`. Historical `context_id = clueless` rows normalize to Standard. Other games retain one leaderboard per game, the `clueless` id and local storage key remain stable (D-028), and play still works offline if the optional backend or migration is unavailable (D-016, D-025). | Superseded by D-057 |
| D-048 | 2026-08-25 | **One signed-in team + live-race layer for every title**, private invite-only, dual unlock | Owner asked to gamify beyond solo campaigns: a crew races the same numbered row (2–4, simultaneous, best individual score). Guests keep solo Play (D-004, D-016). Teams have no public directory. Wordfall-style numbered path with locks; team cursor advances if anyone completes, personal cursor only if that player completes. Clueless daily stays UTC-solo; team Clueless is a timed path that skips today's puzzle. More or Less solo stays endless; live uses a bundled catalog. Live results never write `global_leaderboard`. Engines stay pure. Reddit is out of scope. | Active — Clueless stream clause superseded by D-057; live roster clause narrowed by D-055 |
| D-049 | 2026-08-25 | **A D-041 version-only bump of `package.json` / `app.json` is changelog, not a stack or system change** | The weekly Wikipedia rotate (D-036) writes a new changelog section, bumps those two version fields, then runs `npm run check` on the dirty tree because `GITHUB_TOKEN` PRs do not retrigger CI. The docs audit treated any `package.json` / `app.json` path as stack/system, so the job demanded HOW-IT-WORKS and STACK on a content snapshot — and a content-only PR would fail the same way on merge. The audit now parses the JSON: when only the declared version changed, CHANGELOG remains required and HOW-IT-WORKS / STACK do not. Any other edit to those manifests still requires the stack/system docs. | Active |
| D-050 | 2026-08-25 | **Web chrome uses the visible viewport, and the deer plays only the bundled file until lottie.host drops its white plate** | Phone browsers clip a `100vh` shell under the URL bar, and the hub list had no `flex: 1`, so Wordfall sat under the mascot. `patch-web-head.mjs` plus `ensureWebViewport` set `100dvh` and ink on `html,body`. The phone-frame bezel draws only when the window is wider than `frame.maxWidth`. Separately, lottie.host still serves Shape Layer 1 (a full-canvas white rect). `Mascot` therefore resolves the bundler URL before `Image.resolveAssetSource` and does not fall back to that CDN — a miss is an empty 4:3 slot. D-032's Lottie choice is unchanged; flip the fallback back once the hosted file is re-uploaded. | Superseded in part by D-051 — the fake phone bezel is gone |
| D-051 | 2026-08-25 | **Phone viewports are full-bleed; laptop viewports are a wide column — not a 460px phone mock** | The old `frame.maxWidth: 460` plus `alignItems: 'center'` shrink-wrapped the app on a real phone (percentage width against an indefinite parent) and kept a laptop looking like a device bezel. Below 720px the shell stretches to the window. At 720px and up it is a centered 1080px column with hub cards in a row and Teams as roster + picker columns. `100dvh` from D-050 still applies. | Active |
| D-052 | 2026-08-25 | **Weekly Wikipedia job enqueues a new labelled set from a pipeline reservoir; the player is not advanced by the calendar** | Owner asked for new Wikipedia names rather than the same 50 forever, a come-back loop, and a rounds-passed score next to streak. Passing a round means seeing every name in the current set. The next set stays locked until then. A keyword reservoir lives in `pipeline/keywords/` only (D-004: do not bundle it; the first build yielded 14,112 clean unique titles from Wikimedia tops, short of 20k without padding). Monday cron still opens a review PR, never `master`, and never fetches at play time (D-036). It re-measures shipped items and appends one unused round of ~25 playable articles. Solo Expo/web play that round; team/live races keep the union so a match is not blocked on personal progress (D-048). Reddit uses the newest published round so everyone on a post still shares questions (D-042). | Active |
| D-053 | 2026-08-25 | **Weekly player email is a Tuesday Resend Broadcast, not an Auth template and not a runtime send** | Owner asked to mail signed-in players about game updates, in the branded HTML already written for magic-link, on a Tuesday cadence that follows Wordfall’s Monday drop. Supabase Auth templates cannot broadcast. The pipeline (`pipeline/player-email.ts`) lists confirmed Auth users with the secret key, upserts them into the Resend segment `WordKrush players` without flipping `unsubscribed`, and creates one Broadcast (`send: true`). The same broadcast name is not sent twice. Resend’s `{{{RESEND_UNSUBSCRIBE_URL}}}` is the opt-out. `RESEND_API_KEY` lives in `.env` and on the GitHub Environment `best-games` (the Tuesday job sets `environment: best-games`) — never `EXPO_PUBLIC_*`, never Railway. No `resend` npm package; `fetch` against `api.resend.com`. Guests are not in the list. | Superseded in part by D-054 — the static `whats-new` fallback is gone; transport, Tuesday cadence, and segment remain |
| D-054 | 2026-08-25 | **Tuesday player copy is this week’s changelog + Wordfall, drafted once by OpenAI, not a repeating static letter** | Owner rejected a static roundup fallback: that would resend the same three bullets every quiet week. Facts now come from player-facing `docs/CHANGELOG.md` bullets in a 7-day lookback (14 in `whats-new` mode) plus `isNewestRelease` Wordfall. Infra, tests, snapshots, and pipeline bullets are dropped. Quiet weeks skip. OpenAI (`gpt-4o-mini` via `fetch`, optional `OPENAI_EMAIL_MODEL`) writes player voice from those facts only — one call per week, never per recipient, never at game runtime (D-010 still holds for the validator). Resend merge tags personalize the same HTML: `{{{contact.first_name|there}}}` from Auth username, `{{{game|the games}}}` from the latest `global_scores` row. Broadcast name is `WordKrush weekly YYYY-MM-DD` (that Monday). A missing `OPENAI_API_KEY` fails a real send; a failed model call falls back to a deterministic draft of the same facts. `OPENAI_API_KEY` joins Resend/Supabase on GitHub Environment `best-games`. No `openai` npm package. | Superseded in part by D-062 — facts, quiet-week skip, and merge tags remain; the model call is OpenRouter |
| D-055 | 2026-08-25 | **A live race is 2–10 simultaneous players**, narrowing D-048's 2–4 roster | Owner asked to raise the crew size and to tell players. Min stays 2. Max is 10 in `LIVE_MAX_PLAYERS`, `join_match`, and `start_match`. Teams, lobby, and the Race CTA copy that range. Analytics `player_count_bucket` follows 2–10. Team invite, CRUD, and dual unlock are unchanged. Migration `0008_live_roster_ten.sql` replaces the RPCs so a project that already applied 0006 at the old cap does not need a reset. | Active |
| D-056 | 2026-08-25 | **Wordfall keeps React Native `PanResponder` for every target, with web-only pointer capture and CSS scoped to the board** | Desktop mice and iPad pointer/trackpad gestures otherwise begin browser text selection over a tile before the responder owns the trace. `BoardView` requests the responder in capture phase; `#wk-wordfall-board` disables `user-select`, touch callouts, and `touch-action` only within the play surface. Phone tracing is unchanged, and the app does not add a web-only gesture dependency. | Active |
| D-057 | 2026-08-25 | **Clueless is a local-midnight solo path with separate team content and a human-reviewed daily authoring loop** | Owner replaced the UTC daily difficulty picker with three introductory levels (opening hint, 15-guess hint, no hint) and completion-calendar Daily Vaults. Completion schedules exactly one future level at the player's next local midnight; it neither fetches nor advances on a server calendar. Assistance still partitions score contexts (`easy`, `standard`, `expert`) so old and new runs stay fair, but it is derived from level metadata rather than selected. Team races reserve a separate answer stream, eliminating the old “skip today” spoiler filter. The cache-backed validator appends one future solo puzzle without a runtime model call and rejects exact or near-semantic duplicates. The intended 18:00 GMT+1 Cursor Automation opens or updates the standing `content/clueless-daily` review PR; it never auto-merges, pushes to `master`, calls EAS, or promises content to stale native builds. | Active — daily merge-policy clause superseded by D-059; path design remains active |
| D-058 | 2026-08-25 | **Wordfall maintains its weekly buffer through the standing `content/wordfall-weekly` review branch** | Owner chose a recurring content branch over a Superthread card for every Monday. The authoring loop checks the catalog first, makes no write when four future Mondays already exist, and otherwise appends enough individually designed, fingerprint-unique rows to restore the buffer. An open review PR is extended; when none exists, the branch starts from current `master` and opens one only after local tests, production-export playtest, changelog, and version checks pass. This is a narrow automation exception to D-021, not a general bypass: it never pushes to `master`, auto-merges, calls EAS, or fetches at play time. One unmerged review PR has one pending release version even when later runs extend its buffer. | Active — PR merge-policy clause superseded by D-059; buffer design remains active |
| D-059 | 2026-08-25 | **Labeled Cursor content PRs merge through a GitHub Action after exact-head CI succeeds** | Owner asked both recurring Cursor content loops to merge after CI instead of waiting in a human queue. The automation labels only `content/clueless-daily` and `content/wordfall-weekly` PRs with `automation:auto-merge` and makes them non-draft. `merge-labeled-content.yml` accepts no other branch or label, checks the CI run belongs to the current PR head, blocks drafts and outstanding requested changes, and performs a normal PR merge only after success. A merge performed with `GITHUB_TOKEN` would suppress the normal `master` CI, Railway deploy, and release workflow, so `CONTENT_AUTOMERGE_TOKEN` is a fine-grained PAT or GitHub App token stored only in GitHub Actions secrets. The workflow fails closed without it and never checks out PR code from its write-token `pull_request_target` run. A failed, pending, cancelled, unmergeable, or stale PR stays open. Wikipedia retains human review; EAS and direct pushes to `master` remain out of scope. | Active — draft-readiness clause superseded by D-060; token configured; Automations editor setup remains |
| D-060 | 2026-08-29 | **The content auto-merge label owns draft-to-ready, and GitHub Actions run on Node 24-native releases** | Applying `automation:auto-merge` is the explicit approval signal, so requiring the authoring automation to make a second readiness call left correctly labelled drafts stalled. The trusted `pull_request_target` workflow now validates the open PR's base, same-repository head, named content branch, and label before using `markPullRequestReadyForReview`; it returns after that mutation so the resulting `ready_for_review` event re-evaluates exact-head CI before merge. The workflow still never checks out PR code or accepts another branch. Official actions move to their current Node 24-native majors (`checkout`/`setup-node` v7, `github-script` v9, `create-pull-request` v8) instead of relying on GitHub's temporary Node 20 compatibility override. | Active |
| D-061 | 2026-08-30 | **Branded search uses static files from `patch-web-head.mjs`; D-006 stays (no Next.js split)** | `site:wordkrush.com` returned nothing, so a search for the domain showed a 2014 Orlando media WordPress. The Expo single-page export remains the playable `/`. The existing `build:web` patch now emits a game-named title, canonical, WebSite/WebApplication JSON-LD, hub copy matching `registry.ts`, `robots.txt`, and a one-URL `sitemap.xml`. `serve.mjs` types `.xml` as `application/xml`. Per-game ranking pages and a marketing landing in front of play are out of scope. Google Search Console verification and the indexing request stay an owner step after Railway deploy. | Active |
| D-062 | 2026-08-30 | **Tuesday player-email draft uses OpenRouter, not OpenAI** | The Tuesday job failed closed because GitHub Environment `best-games` has no `OPENAI_API_KEY`. Owner asked to use `OPENROUTER_API_KEY` (already in `.env`) and the OpenRouter base URL. The weekly letter still drafts once from changelog + Wordfall facts via `fetch` + JSON schema, default model `openai/gpt-4o-mini`, default base `https://openrouter.ai/api/v1`. Optional `OPENROUTER_BASE_URL` / `OPENROUTER_EMAIL_MODEL`. A missing `OPENROUTER_API_KEY` fails a real send; a failed model call still falls back to the deterministic draft. Validator keeps `OPENAI_API_KEY` (D-010). No `openai` npm package. | Active |
| D-063 | 2026-08-30 | **WordKrush judgment work runs as specialist Grok bots (Cursor Cloud Agents + `.cursor/skills/`). GitHub Actions stay the deterministic hands. Acquisition in this OS is organic X only.** | Owner asked for bots that run the product. A single god-prompt would ignore existing skills, caps, and CI gates. The split already works: Clueless/Wordfall skills + `merge-labeled-content.yml`, Wikipedia Action + human merge, X growth skill with a ledger. Moving Claude X growth onto Grok is a runtime port, not a new channel. Reddit/Devvit stay in the repo (D-042) but are not scheduled; paid stays off (G-004); remote flags stay off (D-024). Costs accepted: Repo-side Cursor Automations must be configured in the Agents Window; X still needs a logged-in browser and the ledger; owner still merges feature PRs and Wikipedia. **Fleet:** Clueless (Grok 8:55 daily, skill committed, Agents Window owner-configured), Wordfall (Grok Monday 10:55 London, skill committed, Agents Window owner-configured), Conductor (Grok weekdays 9:55 read-only pulse, skill [PLANNED], Agents Window owner-configured), WordKrush QA/Product Health/WordKrush Dev (on demand, playbooks [PLANNED]), WordKrush X (organic X @WordKrushGame, on demand, `.claude/skills/wordkrush-x-growth/` committed, Cursor port [PLANNED]). **Contract:** `docs/AGENT-OS.md` names each bot, skill path, trigger, write surface, and stop conditions. A bot does not replace CI, does not push to master, and does not call EAS. Owner-only work (Apple, Expo login, migrations, [OPEN]/[DECIDE], remote flags, Wikipedia merge, secrets) stops the bot. Conductor does not write `src/`. WordKrush X uses caps (5 posts per 7 days) + ledger + owner posts manually. | Active |


## How to update this document

1. Add a new row to the Decision Log. Never edit an old row's rationale — if a decision is reversed, mark the old row `Superseded by D-0NN` and add the new one.
2. Update the **Current stack** table to match.
3. Update **Last updated** at the top.
4. If the change affects the game design, mirror it in `docs/BRAINSTORM.md`.
